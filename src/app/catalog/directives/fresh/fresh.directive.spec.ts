import { FreshDirective } from './fresh.directive';

describe('FreshDirective', () => {
  it('should create an instance', () => {
    const directive = new FreshDirective();
    expect(directive).toBeTruthy();
  });
});
